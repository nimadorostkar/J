# TokenVault — سیستم معرفی و توزیع پاداش به دعوت‌کنندگان

> **هدف این سند:** توضیح دقیق نحوه کارکرد شبکه دو سطحی معرفی، چگونگی توزیع پاداش به دعوت‌کنندگان در سطح اول (L1) و سطح دوم (L2)، و قوانین حاکم بر «واجد شرایط» بودن یک معرفی.

---

## ۱. درخت دو سطحی معرفی

TokenVault از یک **گراف معرفی دو سطحی** پشتیبانی می‌کند:

| سطح | چه کسی است | ذخیره‌سازی |
|-----|-----------|-----------|
| **L1 (مستقیم)** | کسی که با **کد دعوت شما** ثبت‌نام کرده | `Referral(level=1)` |
| **L2 (غیرمستقیم)** | کسی که با **کد دعوت L1 شما** ثبت‌نام کرده | `Referral(level=2)` |

هر کاربر در زمان ثبت‌نام یک `referral_code` منحصربه‌فرد ۸ کاراکتری دریافت می‌کند. گراف در جدول `referrals.Referral` ذخیره می‌شود و یک محدودیت یکتایی روی `(inviter, invited_user, level)` وجود دارد — یک نفر نمی‌تواند دو بار در همان سطح شمارش شود.

**مثال درخت:**

```
علی (شما)
├── مهدی   ← L1 علی (با کد علی ثبت‌نام کرده)
│   ├── داوود  ← L2 علی / L1 مهدی (با کد مهدی ثبت‌نام کرده)
│   └── سارا   ← L2 علی / L1 مهدی
└── نازنین ← L1 علی
    └── کمال  ← L2 علی / L1 نازنین
```

---

## ۲. وضعیت‌های یک معرفی

همه معرفی‌ها یکسان نیستند. چهار وضعیت پیشرفت وجود دارد:

| وضعیت | شرط |
|--------|-----|
| `registered` | کاربر دعوت‌شده ثبت‌نام کرده — یک ردیف `Referral` وجود دارد |
| `verified` | + ایمیل تأیید شده (`is_email_verified = True`) |
| `first_deposit_completed` | + حداقل یک واریز USDT تکمیل‌شده دارد |
| `qualified` | + معرفی سطح ۱ است (فقط دعوت‌های مستقیم می‌توانند «واجد شرایط» باشند) |

> **قانون ضد حساب جعلی:** فقط معرفی‌های L1 که کاربر دعوت‌شده‌شان حداقل یک واریز تکمیل کرده باشد، برای پاداش‌های مایلستون و فعال‌سازی چرخه پاداش محاسبه می‌شوند. صرف ثبت‌نام هیچ سودی برای دعوت‌کننده ندارد.

---

## ۳. دو روش کسب درآمد از معرفی

### ۳.۱ پاداش مایلستون (بونوس ثابت)

به ازای هر **۵ معرفی L1 واجد شرایط**، دعوت‌کننده یک بونوس ثابت **+۱ H Coin** دریافت می‌کند.

| تنظیم | مقدار پیش‌فرض |
|--------|--------------|
| `REFERRAL_MILESTONE_SIZE` | ۵ |
| `REFERRAL_MILESTONE_REWARD_HCOIN` | ۱ H Coin |

#### زمان فعال‌سازی:

۱. **هنگام ثبت‌نام** — `pay_referral_milestones(inviter)` بلافاصله بعد از ایجاد L1 جدید اجرا می‌شود. چون کاربر جدید هنوز واریزی نکرده، عملاً چیزی پرداخت نمی‌شود — این برای جلوگیری از خطا است.
۲. **هنگام اولین واریز** — `on_deposit_completed(invited_user)` دعوت‌کننده L1 را پیدا کرده و `pay_referral_milestones(inviter)` را صدا می‌زند. **این رویداد اصلی واجد شرایط شدن است.**

#### ایمنی در برابر تکرار:

جدول `ReferralMilestoneReward` دارای `UniqueConstraint(user, milestone)` است. حتی در شرایط همزمانی، یک مایلستون نمی‌تواند دو بار پرداخت شود. یک `IntegrityError` پایگاه داده بی‌صدا نادیده گرفته می‌شود.

#### اتفاقات هنگام رسیدن به مایلستون N:

| اقدام | اثر |
|-------|-----|
| اعتبار موجودی | `h_coin_balance += ۱ H` |
| ردیف تراکنش | `type=referral_milestone, status=completed` |
| رویداد WebSocket | `referral_milestone` + `balance_update` |
| اعلان | "شما به N معرفی رسیدید و ۱ H Coin کسب کردید." |

---

### ۳.۲ کمیسیون از دریافت پاداش (درصد از سود)

وقتی یک کاربر دعوت‌شده **چرخه پاداش ۱۵ روزه خود را دریافت می‌کند**، تابع `distribute_commission(user, profit)` **درون همان تراکنش اتمیک پایگاه داده** اجرا شده و موارد زیر را پرداخت می‌کند:

| سطح | چه کسی دریافت می‌کند | نرخ |
|-----|---------------------|-----|
| **کمیسیون L1** | دعوت‌کننده مستقیم (یک سطح بالاتر) | **۵٪** از مقدار پاداش دریافت‌شده |
| **کمیسیون L2** | جد دعوت‌کننده (دو سطح بالاتر) | **۳٪** از همان مقدار |

> **نکته کلیدی:** کمیسیون از کیف پول کاربر دریافت‌کننده کسر نمی‌شود. این H Coin جدیدی است که توسط پلتفرم از طریق تابع `distribute_commission()` ضرب می‌شود. کاربر دعوت‌شده ۱۰۰٪ پاداش خود را نگه می‌دارد.

#### تنظیمات:

```
REFERRAL_L1_COMMISSION_PCT=5
REFERRAL_L2_COMMISSION_PCT=3
```

#### هر کمیسیون چه ایجاد می‌کند:

- یک ردیف `Transaction(type=commission, commission_level=1 یا 2, commission_rate=5% یا 3%, commission_from_user=<کاربر دریافت‌کننده>)`.
- `Referral.total_commission_earned_hcoin` روی ردیف معرفی افزایش می‌یابد (برای آمار تمام عمر).
- یک رویداد WebSocket `commission_received` و یک اعلان `commission` برای دعوت‌کننده ارسال می‌شود.

---

## ۴. جریان دقیق: چه کسی چه مقدار دریافت می‌کند

### سناریو الف — مهدی (L1 علی) پاداش ۱۰۰ H دریافت می‌کند

```
مهدی پاداش ۱۰۰ H را دریافت می‌کند
│
├── کیف پول مهدی: +۱۰۰ H  (پاداش)
│
└── distribute_commission(مهدی, ۱۰۰)
    ├── علی (دعوت‌کننده L1 مهدی): +۵ H  [۵٪ × ۱۰۰]  → کمیسیون سطح ۱
    └── (کمیسیون L2 ندارد — علی دعوت‌کننده‌ای بالاتر از خودش ندارد)
```

### سناریو ب — داوود (L2 علی، L1 مهدی) پاداش ۲۰۰ H دریافت می‌کند

```
داوود پاداش ۲۰۰ H را دریافت می‌کند
│
├── کیف پول داوود: +۲۰۰ H  (پاداش)
│
└── distribute_commission(داوود, ۲۰۰)
    ├── مهدی (دعوت‌کننده مستقیم L1 داوود): +۱۰ H  [۵٪ × ۲۰۰]  → کمیسیون سطح ۱
    └── علی (جد دعوت‌کننده L2 داوود):      +۶ H  [۳٪ × ۲۰۰]  → کمیسیون سطح ۲
```

هر دو کمیسیون به صورت اتمیک اجرا می‌شوند — یا هر دو موفق می‌شوند یا هیچ‌کدام.

---

## ۵. قفل فعال‌سازی چرخه پاداش (چرا به معرفی نیاز دارید)

قبل از اینکه کاربری بتواند یک چرخه پاداش ۱۵ روزه فعال کند، **هر دو** شرط زیر باید صادق باشند:

۱. `h_coin_balance > 0`
۲. کاربر حداقل **۱ معرفی L1 واجد شرایط** دارد (L1 که حداقل یک واریز انجام داده باشد)

اگر هر یک از شرایط برقرار نباشد، `POST /api/v1/reward/cycle/activate/` با کد `400` و آرایه `reasons[]` برمی‌گردد. رابط کاربری دکمه را قبل از کلیک غیرفعال می‌کند (از فیلد `ineligibilityReasons` در `GET /api/v1/reward/cycle/` گرفته می‌شود).

این یعنی: **برای باز کردن چرخه پاداش ۱۵ روزه، باید حداقل یک نفر را دعوت کنید که واقعاً واریز داشته باشد.**

---

## ۶. اندپوینت آمار

`GET /api/v1/referrals/stats/` تصویر کاملی ارائه می‌دهد:

```json
{
  "l1Count": 12,
  "l2Count": 4,
  "qualifiedCount": 7,
  "pendingDepositCount": 5,
  "milestone": {
    "size": 5,
    "rewardHcoin": "1",
    "milestonesPaid": 1,
    "totalRewardEarnedHcoin": "1",
    "nextMilestoneAt": 10,
    "qualifiedUntilNext": 3,
    "progressPercent": 40.0,
    "qualifyingRule": "فقط کاربران دعوت‌شده‌ای که حداقل یک واریز تکمیل‌شده دارند برای پاداش مایلستون محاسبه می‌شوند."
  }
}
```

---

## ۷. خلاصه کامل درآمدها

| محرک | چه کسی درآمد دارد | مقدار | نوع تراکنش |
|------|------------------|--------|------------|
| هر ۵ واریز اول L1 واجد شرایط (۵، ۱۰، ۱۵، ...) | دعوت‌کننده | +۱ H Coin ثابت | `referral_milestone` |
| هر L1 پاداش دریافت می‌کند | دعوت‌کننده مستقیم آن (والد L1) | +۵٪ از H دریافتی | `commission` (سطح ۱) |
| هر L2 پاداش دریافت می‌کند | جد دعوت‌کننده آن (والد L2) | +۳٪ از H دریافتی | `commission` (سطح ۲) |

> **مهم:** معرفی‌های L2 در شمارش مایلستون دعوت‌کننده محاسبه نمی‌شوند. مایلستون‌ها فقط L1های واجد شرایط را می‌شمارند. L2ها فقط هنگامی درآمد کمیسیونی ایجاد می‌کنند که پاداش دریافت کنند.

---

## ۸. جدول مقایسه L1 در برابر L2

| ویژگی | L1 (مستقیم) | L2 (غیرمستقیم) |
|--------|------------|----------------|
| چگونه اضافه می‌شود | با کد دعوت شما ثبت‌نام کرده | با کد دعوت L1 شما ثبت‌نام کرده |
| می‌تواند «واجد شرایط» شود؟ | بله (بعد از اولین واریز) | خیر — L2 هرگز واجد شرایط نمی‌شود |
| در مایلستون‌ها می‌شمارد؟ | بله (اگر واجد شرایط باشد) | خیر |
| چرخه پاداش شما را فعال می‌کند؟ | بله (حداقل ۱ L1 واجد شرایط لازم است) | خیر |
| کمیسیون هنگام دریافت پاداش؟ | بله — ۵٪ به شما | بله — ۳٪ به شما |
| در شبکه نشان داده می‌شود؟ | بله | بله |

---

## ۹. محل کد

| موضوع | فایل |
|--------|------|
| گراف معرفی و وضعیت‌ها | `backend/referrals/models.py` |
| کوئری‌ست واجد شرایط بودن | `backend/referrals/models.py::ReferralQuerySet` |
| توزیع کمیسیون | `backend/referrals/services.py::distribute_commission` |
| پرداخت مایلستون | `backend/referrals/services.py::pay_referral_milestones` |
| محرک اولین واریز | `backend/referrals/services.py::on_deposit_completed` |
| دریافت چرخه پاداش (محرک کمیسیون) | `backend/rewards/views.py` |
| تنظیمات | `backend/core/settings/base.py` |

---

*آخرین به‌روزرسانی: اردیبهشت ۱۴۰۵ — کدبیس TokenVault.*
