# === FILE: backend/requirements.txt ===
Django>=5.0,<5.2
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4
django-filter>=24.2
django-ratelimit>=4.1
django-storages[s3]>=1.14
channels>=4.1
channels-redis>=4.2
django-celery-beat>=2.6
django-celery-results>=2.5
celery[redis]>=5.4
redis>=5.0
psycopg2-binary>=2.9
Pillow>=10.3
boto3>=1.34
drf-yasg>=1.21
gunicorn>=22.0
daphne>=4.1
whitenoise>=6.7
eth-utils>=4.1
base58>=2.1
cryptography>=42.0
django-encrypted-model-fields>=0.6
python-decouple>=3.8
dj-database-url>=2.2
qrcode[pil]>=7.4
requests>=2.32

# ── Crypto payment gateway ──────────────────────────────────────────
# Outbound (signing) deps. Read paths use plain HTTP against TronGrid
# / Etherscan, so these are only required when GATEWAY_DRY_RUN=False
# and you actually broadcast withdrawals. Uncomment in prod images.
# tronpy>=0.4
# web3>=6.0
# eth-account>=0.10
